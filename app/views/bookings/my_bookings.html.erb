<h1>My bookings:</h1>

<% if @bookings %>
  <ul>
    <% @bookings.each do |booking| %>
      <li><%= booking.bike.brand %> || <%= booking.booking_start %> ||
      <%= booking.booking_end %> || <%= booking.price_per_day %>
      </li>
    <% end %>
  </ul>
<% end %>
